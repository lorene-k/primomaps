[project]
name = "primomaps"
version = "0.1.0"
description = ""
readme = "../README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.141.1",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pip-audit>=2.10.1",
    "ruff>=0.16.4",
]

[tool.mypy]
ignore_missing_imports = true